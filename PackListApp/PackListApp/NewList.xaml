<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PackListApp.NewList"
             Title="New List"
             Style="{StaticResource ColorTheme}">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Grid Style="{StaticResource GridStyle}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"></RowDefinition>
                    <RowDefinition Height="100"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"></ColumnDefinition>
                    <ColumnDefinition Width="100"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Row="0" Grid.Column="0" Text="Quickie"
                        Clicked="QuickBtn_OnClicked" Style="{StaticResource ButtonTheme}"></Button>
                <Button Grid.Row="0" Grid.Column="1" Text="My Own"
                        Clicked="MyOwnBtn_OnClicked" Style="{StaticResource ButtonTheme}"></Button>
                <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="Blank"
                        Clicked="NewBlankBtn_OnClicked" Style="{StaticResource ButtonTheme}"></Button>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>